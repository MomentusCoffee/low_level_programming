#include <stdio.h>
/**
 * main - rename file
 * @argc: int
 * @argv: char
 * Return: new name
 */
int main(int argc, char **argv)
{
	int name = 0;

	if (argc >= 0)
		printf("%s\n", argv[name]);
	return (0);
}
